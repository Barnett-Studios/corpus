id: python-05-split-join-ext-roundtrip
language: python
files: ["util.py"]
accept: "python3 -m unittest -q test_util.TestUtil.test_split_join_ext_roundtrip"
forbid: ["new_deps"]
change: "TWO-REGION change: implement BOTH split_ext and join_ext in util.py. split_ext returns (stem, ext) where ext is the text after the LAST '.' without the dot, or '' when there is no '.'. join_ext is the exact inverse: join with '.', or return just stem when ext is ''. Both must agree. Do not touch any other function."
provenance: "hand-authored"
