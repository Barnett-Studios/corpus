id: rust-01-compact-path
language: rust
files: ["src/lib.rs"]
accept: "cargo test test_compact_path --quiet 2>&1 | grep -qE 'test result: ok\\. [1-9]'"
forbid: ["new_deps"]
change: "Implement ONLY the `compact_path` function body (replace its `todo!()`). Return the path unchanged when it has at most `max` '/'-separated components; otherwise keep the first and last components joined with the middle elided as the single character '\u2026', e.g. compact_path(\"a/b/c/d\", 3) == \"a/\u2026/d\". Do not touch any other function."
