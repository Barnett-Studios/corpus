id: java-03-strip-prefix-dir
language: java
files: ["Util.java"]
accept: "javac -encoding UTF-8 Util.java TestRunner.java 2>&1 && java TestRunner stripPrefixDir 2>&1 | grep -qx PASS"
forbid: ["new_deps"]
change: "Implement ONLY the body of stripPrefixDir in Util.java. Strip a leading 'prefix/' directory from path; if path does not start with 'prefix/' (note the trailing slash) return it unchanged. Do not touch any other method."
