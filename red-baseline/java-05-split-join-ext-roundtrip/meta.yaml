id: java-05-split-join-ext-roundtrip
language: java
files: ["Util.java"]
accept: "javac -encoding UTF-8 Util.java TestRunner.java 2>&1 && java TestRunner splitJoinExt 2>&1 | grep -qx PASS"
forbid: ["new_deps"]
change: "TWO-REGION change: implement BOTH splitExt and joinExt in Util.java. splitExt returns a String[]{stem, ext} where ext is the text after the LAST '.' without the dot, or '' when there is no '.'. joinExt is the exact inverse: join with '.', or return just stem when ext is ''. Both must agree. Do not touch any other method."
