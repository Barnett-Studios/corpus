id: python-04-encode-decode-roundtrip
language: python
files: ["util.py"]
accept: "python3 -m unittest -q test_util.TestUtil.test_encode_decode_roundtrip"
forbid: ["new_deps"]
change: "TWO-REGION change: implement BOTH encode_seg and decode_seg in util.py so decode_seg(encode_seg(s))==s for any input. encode_seg replaces every '/' with the literal '%2F'; decode_seg reverses it exactly. Implementing only one leaves the other raising. Do not touch any other function."
